import { MdCheckCircle } from "react-icons/md";
import IconWrapper from "./wrapper";

const SuccessIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdCheckCircle,
});

export default SuccessIcon;
