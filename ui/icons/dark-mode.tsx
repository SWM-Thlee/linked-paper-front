import { MdDarkMode } from "react-icons/md";
import IconWrapper from "./wrapper";

const DarkModeIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdDarkMode,
});

export default DarkModeIcon;
