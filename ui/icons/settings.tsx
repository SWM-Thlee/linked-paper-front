import { MdSettings } from "react-icons/md";
import IconWrapper from "./wrapper";

const SettingsIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdSettings,
});

export default SettingsIcon;
