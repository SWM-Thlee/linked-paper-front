import { MdLightMode } from "react-icons/md";
import IconWrapper from "./wrapper";

const LightModeIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdLightMode,
});

export default LightModeIcon;
