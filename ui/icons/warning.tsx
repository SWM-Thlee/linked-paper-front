import { MdWarning } from "react-icons/md";
import IconWrapper from "./wrapper";

const WarningIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdWarning,
});

export default WarningIcon;
