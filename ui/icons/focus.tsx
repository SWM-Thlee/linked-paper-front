import { MdCenterFocusWeak } from "react-icons/md";
import IconWrapper from "./wrapper";

const FocusIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdCenterFocusWeak,
});

export default FocusIcon;
