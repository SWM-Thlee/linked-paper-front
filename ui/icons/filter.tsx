import { MdFilterList } from "react-icons/md";
import IconWrapper from "./wrapper";

const FilterIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdFilterList,
});

export default FilterIcon;
