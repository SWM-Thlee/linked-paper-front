import { MdTipsAndUpdates } from "react-icons/md";
import IconWrapper from "./wrapper";

const TipIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdTipsAndUpdates,
});

export default TipIcon;
