import { MdMenuBook } from "react-icons/md";
import IconWrapper from "./wrapper";

const JournalIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdMenuBook,
});

export default JournalIcon;
