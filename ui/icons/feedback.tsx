import { MdMessage } from "react-icons/md";
import IconWrapper from "./wrapper";

const FeedbackIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdMessage,
});

export default FeedbackIcon;
