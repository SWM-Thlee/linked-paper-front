import { MdListAlt } from "react-icons/md";
import IconWrapper from "./wrapper";

const OverviewIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdListAlt,
});

export default OverviewIcon;
