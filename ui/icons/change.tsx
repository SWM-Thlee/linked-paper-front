import { MdChangeCircle } from "react-icons/md";
import IconWrapper from "./wrapper";

const ChangeIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdChangeCircle,
});

export default ChangeIcon;
