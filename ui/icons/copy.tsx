import { MdContentCopy } from "react-icons/md";
import IconWrapper from "./wrapper";

const CopyIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdContentCopy,
});

export default CopyIcon;
