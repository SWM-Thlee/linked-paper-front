import { MdHistory } from "react-icons/md";
import IconWrapper from "./wrapper";

const HistoryIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdHistory,
});

export default HistoryIcon;
