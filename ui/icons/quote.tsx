import { MdFormatQuote } from "react-icons/md";
import IconWrapper from "./wrapper";

const QuoteIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdFormatQuote,
});

export default QuoteIcon;
