import { MdWifiTethering } from "react-icons/md";
import IconWrapper from "./wrapper";

const BloomIcon = IconWrapper({
  type: "react-icons",
  baseComponent: MdWifiTethering,
});

export default BloomIcon;
