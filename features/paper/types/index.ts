import * as SchemeTypes from "./scheme";
import * as SimilarityTypes from "./similarity";

export namespace Paper {
  export import Scheme = SchemeTypes;
  export import Similarity = SimilarityTypes;
}
